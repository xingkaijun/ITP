# NBINS 身份中心 API 地址（不带末尾斜杠）。
# 配置后登录页可用 NBINS 账号（用户名+密码）登录；留空则只能用本地口令。
# 本地开发（wrangler dev）示例：
# VITE_NBINS_API_BASE=http://127.0.0.1:8787
# 生产示例：
# VITE_NBINS_API_BASE=https://<your-nbins-api-domain>
VITE_NBINS_API_BASE=
